#pragma once
#include <string>
#include <iostream>
#include <ctype.h>

static class IO {
public:
	std::string static stringInitial(std::string strVarToWriteTo);
	int static formattedDate();
};